#include <vector>
#include <string>

class freezerayConfigs {
public:
    static bool ifCoreExists();
    static char IfFolderExist();
    static const std::vector<std::string>& GetJsonFiles(); // tf this is
};

class fv2config {
public:
    int configInfo(); // placeholder function name
};
